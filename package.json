{
  "name": "coding-agent-remote-control",
  "displayName": "Coding Agent Terminal With Remote Control",
  "description": "A VS Code extension with tabbed terminals with remote control",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:coding-agent-remote-control.openTerminal"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "coding-agent-remote-control.openTerminal",
        "title": "Open Coding Agent Terminal"
      }
    ],
    "keybindings": [
      {
        "command": "coding-agent-remote-control.openTerminal",
        "key": "cmd+shift+c",
        "mac": "cmd+shift+c"
      }
    ],
    "viewsContainers": {
      "panel": [
        {
          "id": "coding-agent-container",
          "title": "CODING AGENTS",
          "icon": "$(terminal)"
        }
      ]
    },
    "views": {
      "coding-agent-container": [
        {
          "id": "coding-agent-panel",
          "name": "CODING AGENTS",
          "type": "webview",
          "when": "codingAgent.active"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@vscode/test-electron": "^2.2.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^0.10.1"
  }
}
